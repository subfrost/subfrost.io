# Keep at least one CDN pod serving during voluntary disruptions
# (node upgrades, spot preemptions, drains).
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: subfrost-cdn
  namespace: subfrost
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: subfrost-cdn
